import {NextResponse} from "next/server";import type {NextRequest} from "next/server";import {jwtVerify} from "jose";export async function middleware(req:NextRequest){const token=req.cookies.get('zenith_session')?.value;if(!token)return NextResponse.redirect(new URL('/login',req.url));try{const {payload}=await jwtVerify(token,new TextEncoder().encode(process.env.SESSION_SECRET||'development-only-secret-change-me'));const onboarding=req.nextUrl.pathname.startsWith('/onboarding');if(payload.role==='MEMBER'&&payload.onboardingRequired&&!onboarding)return NextResponse.redirect(new URL('/onboarding',req.url));if(onboarding&&(!payload.onboardingRequired||payload.role!=='MEMBER'))return NextResponse.redirect(new URL(payload.role==='ADMIN'?'/admin/import':payload.role==='STAFF'?'/staff':'/dashboard',req.url));if(req.nextUrl.pathname.startsWith('/admin')&&payload.role!=='ADMIN')return NextResponse.redirect(new URL(payload.role==='STAFF'?'/staff':'/dashboard',req.url));if(req.nextUrl.pathname.startsWith('/staff')&&!['STAFF','ADMIN'].includes(String(payload.role)))return NextResponse.redirect(new URL('/dashboard',req.url));return NextResponse.next()}catch{return NextResponse.redirect(new URL('/login',req.url))}}export const config={matcher:['/dashboard/:path*','/points/:path*','/schedule/:path*','/onboarding/:path*','/staff/:path*','/admin/:path*']};
