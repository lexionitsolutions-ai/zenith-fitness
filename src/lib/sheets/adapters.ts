import type {MembershipSheetAdapter,RawMembershipRow} from "@/types/sheets";
export class LocalJsonAdapter implements MembershipSheetAdapter{constructor(private rows:RawMembershipRow[]){}async fetchMembershipRows(){return this.rows}}
export class GoogleAppsScriptAdapter implements MembershipSheetAdapter{constructor(private url=process.env.GOOGLE_SHEETS_API_URL,private secret=process.env.IMPORT_API_SECRET){}async fetchMembershipRows(){if(!this.url)throw new Error("GOOGLE_SHEETS_API_URL is not configured");const r=await fetch(this.url,{headers:this.secret?{"x-import-secret":this.secret}:undefined,cache:"no-store"});if(!r.ok)throw new Error(`Sheets endpoint returned ${r.status}`);const data:unknown=await r.json();const rows=Array.isArray(data)?data:typeof data==='object'&&data!==null&&'members' in data&&Array.isArray(data.members)?data.members:null;if(!rows)throw new Error("Sheets endpoint must return an array or { success, members: [] }");return rows as RawMembershipRow[]}}
