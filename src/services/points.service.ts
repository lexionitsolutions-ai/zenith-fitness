import {Prisma,TargetStatus} from "@prisma/client";import {prisma} from "@/lib/database/prisma";import {AppError} from "@/lib/errors";import {hasActiveMembership,indiaBusinessDate} from "@/lib/utils/point-rules";export {indiaBusinessDate};
export async function awardDailyVisit(qrToken:string,staffUserId:string){const member=await prisma.member.findUnique({where:{qrToken},include:{memberships:true}});if(!member||!member.qrCodeActive)throw new AppError('INVALID_QR','Member QR code is invalid.',404);if(!hasActiveMembership(member.memberships))throw new AppError('MEMBERSHIP_INACTIVE','Daily points are unavailable because this membership is expired or not yet active.',409);const date=indiaBusinessDate(),key=`daily:${member.id}:${date.toISOString().slice(0,10)}`;try{const transaction=await prisma.pointTransaction.create({data:{memberId:member.id,points:5,transactionType:'DAILY_VISIT',referenceKey:key,description:'Daily gym visit',awardedByUserId:staffUserId,businessDate:date}});return {member:{admissionId:member.admissionId,name:member.fullName},transaction,alreadyAwarded:false}}catch(e){if(e instanceof Prisma.PrismaClientKnownRequestError&&e.code==='P2002')return {member:{admissionId:member.admissionId,name:member.fullName},transaction:null,alreadyAwarded:true};throw e}}
export async function getMemberPoints(memberId:string){const [member,transactions,targets,sums,leaders]=await Promise.all([prisma.member.findUnique({where:{id:memberId},select:{qrToken:true,admissionId:true,fullName:true}}),prisma.pointTransaction.findMany({where:{memberId},orderBy:{createdAt:'desc'},take:30}),prisma.memberTarget.findMany({where:{memberId},orderBy:{createdAt:'desc'}}),prisma.pointTransaction.aggregate({where:{memberId},_sum:{points:true}}),prisma.pointTransaction.groupBy({by:['memberId'],_sum:{points:true},orderBy:{_sum:{points:'desc'}},take:20})]);if(!member)throw new AppError('MEMBER_NOT_FOUND','Member not found.',404);const leaderMembers=await prisma.member.findMany({where:{id:{in:leaders.map(l=>l.memberId)}},select:{id:true,fullName:true,admissionId:true}});const byId=new Map(leaderMembers.map(m=>[m.id,m]));return {member,pointsBalance:sums._sum.points??0,transactions,targets,leaderboard:leaders.map((l,i)=>{const m=byId.get(l.memberId)!;return {rank:i+1,name:m.fullName.split(' ')[0],admissionId:`${m.admissionId.slice(0,3)}***${m.admissionId.slice(-2)}`,points:l._sum.points??0,isCurrentMember:l.memberId===memberId}})}}
export async function assignTarget(input:{memberId:string;title:string;description?:string;targetValue?:number;unit?:string;rewardPoints:number;startDate:Date;dueDate:Date},staffUserId:string){if(input.dueDate<input.startDate)throw new AppError('INVALID_TARGET_DATES','Due date must be on or after start date.');return prisma.memberTarget.create({data:{...input,assignedByUserId:staffUserId}})}
export async function completeTarget(targetId:string,staffUserId:string){return prisma.$transaction(async tx=>{const target=await tx.memberTarget.findUnique({where:{id:targetId}});if(!target)throw new AppError('TARGET_NOT_FOUND','Target not found.',404);if(target.status===TargetStatus.COMPLETED)throw new AppError('TARGET_ALREADY_COMPLETED','Target reward has already been awarded.',409);const completed=await tx.memberTarget.update({where:{id:targetId},data:{status:'COMPLETED',completedAt:new Date(),completedByUserId:staffUserId}});await tx.pointTransaction.create({data:{memberId:target.memberId,points:target.rewardPoints,transactionType:'TARGET_COMPLETION',referenceKey:`target:${target.id}`,description:`Target completed: ${target.title}`,awardedByUserId:staffUserId,businessDate:indiaBusinessDate()}});return completed})}
export async function reduceMemberPoints(memberId:string,actorUserId:string,action:'REDUCE'|'RESET',amount:number|undefined,reason:string){return prisma.$transaction(async tx=>{const member=await tx.member.findUnique({where:{id:memberId},select:{id:true,admissionId:true,fullName:true}});if(!member)throw new AppError('MEMBER_NOT_FOUND','Member not found.',404);const total=await tx.pointTransaction.aggregate({where:{memberId},_sum:{points:true}}),balance=Math.max(0,total._sum.points??0);const deduction=action==='RESET'?balance:Math.min(balance,Math.max(0,amount??0));if(deduction<=0)throw new AppError('NO_POINTS_TO_REDUCE',balance===0?'Member balance is already zero.':'Enter a valid reduction amount.',409);const transaction=await tx.pointTransaction.create({data:{memberId,points:-deduction,transactionType:'REVERSAL',description:`${action==='RESET'?'Balance reset':'Points reduced'}: ${reason}`,awardedByUserId:actorUserId,businessDate:indiaBusinessDate()}});return {member,balanceBefore:balance,balanceAfter:balance-deduction,transaction}},{isolationLevel:Prisma.TransactionIsolationLevel.Serializable})}
