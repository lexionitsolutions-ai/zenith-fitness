import {getSession} from "@/lib/auth/session";import {LocalJsonAdapter,GoogleAppsScriptAdapter} from "@/lib/sheets/adapters";import {runImport} from "@/services/migration.service";import {AppError,apiError} from "@/lib/errors";import sample from "@/data/sample-memberships.json";export async function POST(req:Request){try{const s=await getSession();if(s?.role!=='ADMIN')throw new AppError('FORBIDDEN','Administrator access required.',403);const {source}=await req.json() as {source:'local'|'google'};const adapter=source==='local'?new LocalJsonAdapter(sample as never):new GoogleAppsScriptAdapter();return Response.json({success:true,data:await runImport(adapter,s.userId)})}catch(e){return apiError(e)}}
export async function GET(){try{const s=await getSession();if(s?.role!=='ADMIN')throw new AppError('FORBIDDEN','Administrator access required.',403);const {prisma}=await import('@/lib/database/prisma');return Response.json({success:true,data:await prisma.importBatch.findMany({take:10,orderBy:{createdAt:'desc'},include:{logs:{where:{status:{in:['WARNING','FAILED']}},take:50}}})})}catch(e){return apiError(e)}}
