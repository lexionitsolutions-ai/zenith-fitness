import {getSession} from "@/lib/auth/session";import {getMemberPoints} from "@/services/points.service";import {redirect} from "next/navigation";import {PointsView} from "@/components/dashboard/points-view";import {MemberBottomNav} from "@/components/layout/member-bottom-nav";import {ZenithLogo} from "@/components/brand/zenith-logo";export default async function Page(){const s=await getSession();if(!s?.memberId)redirect('/login');return <><div className="app-topbar border-b border-white/10"><div className="safe-x mx-auto flex max-w-3xl items-center px-4 py-3"><ZenithLogo compact/></div></div><PointsView data={await getMemberPoints(s.memberId)}/><MemberBottomNav active="points"/></>}
