import {redirect} from "next/navigation";import {RoleBottomNav} from "@/components/layout/role-bottom-nav";import {ZenithLogo} from "@/components/brand/zenith-logo";import {BirthdayAlert} from "@/components/staff/birthday-alert";import {requireActiveRole} from "@/lib/auth/authorize";export default async function Layout({children}:{children:React.ReactNode}){let s;try{s=await requireActiveRole(['STAFF','ADMIN'])}catch{redirect('/login')}return <div className="pb-28"><div className="mx-auto flex max-w-5xl items-center border-b border-white/10 px-5 py-3"><ZenithLogo compact/></div><BirthdayAlert/>{children}<RoleBottomNav role={s.role==='ADMIN'?'ADMIN':'STAFF'}/></div>}
