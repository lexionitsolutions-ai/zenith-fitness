import {getSession} from "@/lib/auth/session";import {redirect} from "next/navigation";import {BATCH_SCHEDULE,getNextBatch} from "@/constants/batch-schedule";import Link from "next/link";import {ArrowLeft,CalendarDays,Clock} from "lucide-react";export default async function Page(){if(!(await getSession())?.memberId)redirect('/login');const next=getNextBatch(),groups=[1,2,3,4,5,6].map(day=>({day,sessions:BATCH_SCHEDULE.filter(x=>x.day===day)}));return <main className="safe-bottom mx-auto min-h-dvh max-w-3xl px-4 py-6"><Link href="/dashboard" className="inline-flex min-h-11 items-center gap-2 text-white/60"><ArrowLeft size={18}/>Dashboard</Link><header className="mt-5"><p className="text-sm text-zenith-400">WEEKLY PROGRAM</p><h1 className="text-3xl font-black">Batch schedule</h1></header><article className="mt-6 rounded-3xl bg-gradient-to-br from-zenith-500 to-emerald-800 p-6"><p className="text-xs font-bold uppercase tracking-widest text-white/70">Up next</p><h2 className="mt-2 text-2xl font-black">{next.name}</h2><p>{next.dayName} · {new Date(next.startsAt).toLocaleTimeString('en-IN',{timeZone:'Asia/Kolkata',hour:'numeric',minute:'2-digit'})}</p></article><div className="mt-6 space-y-4">{groups.map(g=><section key={g.day} className="rounded-2xl border border-white/10 bg-white/5 p-5"><h2 className="flex items-center gap-2 font-bold"><CalendarDays size={18} className="text-zenith-400"/>{g.sessions[0].dayName}</h2><div className="mt-3 grid gap-2 sm:grid-cols-2">{g.sessions.map((s,i)=><div key={`${s.time}-${i}`} className="flex items-center gap-3 rounded-xl bg-black/20 p-3"><Clock size={17} className="text-white/40"/><div><b>{new Date(`2000-01-01T${s.time}:00`).toLocaleTimeString('en-IN',{hour:'numeric',minute:'2-digit'})}</b><p className="text-sm text-white/55">{s.name}</p></div></div>)}</div></section>)}</div><nav className="fixed bottom-3 left-1/2 z-20 flex w-[calc(100%-24px)] max-w-md -translate-x-1/2 justify-around rounded-2xl border border-white/10 bg-[#10201b]/95 p-2"><Link href="/dashboard" className="min-h-12 rounded-xl px-5 py-3 text-sm text-white/60">Home</Link><Link href="/points" className="min-h-12 rounded-xl px-5 py-3 text-sm text-white/60">Points</Link><span className="min-h-12 rounded-xl bg-zenith-500/20 px-5 py-3 text-sm text-zenith-400">Schedule</span></nav></main>}
