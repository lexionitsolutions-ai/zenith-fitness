import {getSession} from "@/lib/auth/session";import {prisma} from "@/lib/database/prisma";import {redirect} from "next/navigation";import {AnnouncementAdmin} from "@/components/admin/announcement-admin";export default async function Page(){if((await getSession())?.role!=='ADMIN')redirect('/login');const rows=await prisma.announcement.findMany({orderBy:{createdAt:'desc'},take:50});return <AnnouncementAdmin initial={JSON.parse(JSON.stringify(rows))}/>}
