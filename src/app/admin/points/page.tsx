import {getSession} from "@/lib/auth/session";import {prisma} from "@/lib/database/prisma";import {redirect} from "next/navigation";export default async function Page(){if((await getSession())?.role!=='ADMIN')redirect('/login');const rows=await prisma.pointTransaction.findMany({take:100,orderBy:{createdAt:'desc'},include:{member:{select:{fullName:true,admissionId:true}},awardedBy:{select:{mobileNumber:true,role:true}}}});return <main className="mx-auto min-h-dvh max-w-5xl px-5 py-8"><p className="text-sm text-zenith-400">ADMIN AUDIT</p><h1 className="text-3xl font-black">Points ledger</h1><div className="mt-7 overflow-x-auto rounded-2xl border border-white/10"><table className="w-full min-w-[700px] text-left text-sm"><thead className="bg-white/10"><tr>{['Date','Member','Type','Description','Actor','Points'].map(x=><th key={x} className="p-3">{x}</th>)}</tr></thead><tbody>{rows.map(x=><tr key={x.id} className="border-t border-white/5"><td className="p-3">{x.createdAt.toLocaleString('en-IN')}</td><td className="p-3">{x.member.fullName}<small className="block text-white/40">{x.member.admissionId}</small></td><td className="p-3">{x.transactionType}</td><td className="p-3">{x.description}</td><td className="p-3">{x.awardedBy?.role??'SYSTEM'}</td><td className={`p-3 font-bold ${x.points>=0?'text-zenith-400':'text-red-300'}`}>{x.points>0?'+':''}{x.points}</td></tr>)}</tbody></table></div></main>}
