"use client";import {AlertTriangle,CalendarDays,CreditCard,Dumbbell,Home,UserRound} from "lucide-react";import {useRouter} from "next/navigation";
type Data=Awaited<ReturnType<typeof import('@/services/dashboard.service').getDashboard>>;const money=(v:number)=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(v);const date=(v:string|null)=>v?new Intl.DateTimeFormat('en-IN',{day:'numeric',month:'short',year:'numeric'}).format(new Date(v+'T00:00:00Z')):'Needs review';
export function Dashboard({data}:{data:Data}){const router=useRouter(),m=data.membership,p=data.payment;const logout=async()=>{await fetch('/api/auth/logout',{method:'POST'});router.replace('/login');router.refresh()};return <main className="safe-bottom mx-auto min-h-dvh max-w-5xl px-4 pb-28 pt-6 sm:px-8"><header className="flex items-center justify-between"><div><p className="text-sm text-white/50">Good to see you</p><h1 className="text-2xl font-bold">{data.member.name.split(' ')[0]} <span className="text-zenith-400">↗</span></h1><p className="text-xs text-white/40">{data.member.admissionId}</p></div><button onClick={logout} className="h-11 rounded-full border border-white/10 px-4 text-sm">Log out</button></header><section className="mt-8 grid gap-4 md:grid-cols-2"><article className="relative overflow-hidden rounded-3xl bg-gradient-to-br from-zenith-500 to-emerald-800 p-6 shadow-glow"><Dumbbell className="absolute right-5 top-5 text-white/25" size={48}/><p className="text-xs font-bold uppercase tracking-widest text-white/70">{m?.status??'No membership'}</p><h2 className="mt-2 text-3xl font-black">{m?.planName??'Visit the front desk'}</h2><p className="mt-1 text-white/70">{m?.category??'No plan on record'}</p><div className="mt-8 grid grid-cols-2 gap-4 border-t border-white/20 pt-5 text-sm"><div><p className="text-white/60">Starts</p><b>{date(m?.startDate??null)}</b></div><div><p className="text-white/60">Ends</p><b>{date(m?.endDate??null)}</b></div></div>{m?.daysRemaining!=null&&<p className="mt-5 text-sm"><strong className="text-2xl">{Math.max(0,m.daysRemaining)}</strong> days remaining</p>}</article>{p&&<article className="rounded-3xl border border-white/10 bg-white/[.04] p-6"><div className="flex items-center gap-3"><span className="rounded-xl bg-white/10 p-2"><CreditCard/></span><div><p className="text-xs text-white/50">PAYMENT</p><h2 className="font-bold">{p.paymentStatus}</h2></div></div><div className="mt-7 grid grid-cols-3 gap-2"><div><p className="text-xs text-white/45">Total</p><b>{money(p.finalAmount)}</b></div><div><p className="text-xs text-white/45">Paid</p><b>{money(p.amountPaid)}</b></div><div><p className="text-xs text-white/45">Pending</p><b className="text-amber-300">{money(p.pendingAmount)}</b></div></div><p className="mt-6 text-sm text-white/50">Mode: {p.paymentMode??'Not recorded'}</p></article>}</section>{data.alerts.length>0&&<section className="mt-7 space-y-3"><h2 className="font-bold">For you</h2>{data.alerts.map(a=><article key={a.type} className="flex gap-3 rounded-2xl border border-amber-400/20 bg-amber-400/[.07] p-4"><AlertTriangle className="shrink-0 text-amber-300" size={20}/><div><b className="text-sm">{a.title}</b><p className="text-sm text-white/55">{a.message}</p></div></article>)}</section>}<section className="mt-7 rounded-3xl border border-white/10 bg-white/[.04] p-6"><div className="flex items-center justify-between"><h2 className="font-bold">Profile</h2><span className="text-sm text-zenith-400">{data.member.profileCompletionPercentage}% complete</span></div><dl className="mt-5 grid grid-cols-2 gap-4 text-sm"><div><dt className="text-white/40">Member</dt><dd>{data.member.name}</dd></div><div><dt className="text-white/40">Mobile</dt><dd>{data.member.mobile??'Missing'}</dd></div><div><dt className="text-white/40">Gender</dt><dd>{data.member.gender??'Missing'}</dd></div><div><dt className="text-white/40">Birth date</dt><dd>{data.member.birthDate??'Missing'}</dd></div></dl></section><nav className="fixed bottom-3 left-1/2 flex w-[calc(100%-24px)] max-w-md -translate-x-1/2 justify-around rounded-2xl border border-white/10 bg-[#10201b]/95 px-2 py-2 shadow-2xl backdrop-blur"><button className="flex min-h-12 flex-col items-center justify-center px-5 text-xs text-zenith-400"><Home size={20}/>Home</button><button className="flex min-h-12 flex-col items-center justify-center px-5 text-xs text-white/60"><UserRound size={20}/>Profile</button><button disabled className="flex min-h-12 flex-col items-center justify-center px-5 text-xs text-white/25"><CalendarDays size={20}/>Soon</button></nav></main>}
