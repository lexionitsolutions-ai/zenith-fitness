export type BatchSession={day:number;dayName:string;time:string;hour:number;minute:number;name:string};const days=['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'];const rows:[number,string,string][]=[[1,'06:30','Zumba'],[1,'18:00','Zumba'],[2,'08:00','Functional'],[2,'18:00','Functional'],[2,'19:00','Functional'],[3,'06:00','Masters Batch'],[3,'07:00','Masters Batch'],[3,'18:00','Mobility'],[4,'08:00','Functional'],[4,'18:00','Functional'],[5,'06:00','Functional'],[5,'07:00','Masters Batch'],[5,'19:00','Masters Batch'],[6,'06:00','Mobility Training'],[6,'07:00','Yoga'],[6,'19:00','Yoga']];export const BATCH_SCHEDULE:BatchSession[]=rows.map(([day,time,name])=>{const [hour,minute]=time.split(':').map(Number);return {day,dayName:days[day],time,hour,minute,name}});
const indiaParts=(d:Date)=>{const p=new Intl.DateTimeFormat('en-US',{timeZone:'Asia/Kolkata',weekday:'short',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',hourCycle:'h23'}).formatToParts(d);const get=(t:string)=>p.find(x=>x.type===t)?.value??'';return {year:+get('year'),month:+get('month'),date:+get('day'),hour:+get('hour'),minute:+get('minute'),weekday:['Sun','Mon','Tue','Wed','Thu','Fri','Sat'].indexOf(get('weekday'))}};
export function getNextBatch(now=new Date()){const local=indiaParts(now);for(let offset=0;offset<8;offset++){const targetDay=(local.weekday+offset)%7;for(const batch of BATCH_SCHEDULE.filter(x=>x.day===targetDay)){if(offset===0&&(batch.hour<local.hour||batch.hour===local.hour&&batch.minute<=local.minute))continue;const localMidnight=new Date(Date.UTC(local.year,local.month-1,local.date+offset));const startsAt=new Date(Date.UTC(localMidnight.getUTCFullYear(),localMidnight.getUTCMonth(),localMidnight.getUTCDate(),batch.hour-5,batch.minute-30));return {...batch,startsAt}}}throw new Error('Batch schedule is empty')}
